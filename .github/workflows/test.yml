name: test 
on: push
jobs: 
  log-date: 
    runs-on: ubuntu-latest
    steps:
      - name: milog date
        run: date
      - name: milog whoami
        run: whoami
  log-node-version:
    needs: milog-date
    runs-on: ubuntu-latest
    steps:
      - name: log node version
        run: node --version   